task_config 
{
  iou_thres: 0.5
  num_keys: 17
  bucket_size: 5.0     # cm
  max_distance: 3000    # cm
 
  # 离线调试时保存预处理输出数据和engine推理数据
  run_status: 0      # （0:在线运行（默认）； 1：离线调试）
}

modules_config {
  modules {
    type: "preprocess"
    name: "Location"
  }
}